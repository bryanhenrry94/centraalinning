// Shared vertical rhythm for every signup step (island / plan / details),
// so spacing stays identical across screens.
export const STEP_HEADER_GAP = 0.5; // title -> subtitle
export const STEP_SECTION_GAP = 5; // stepper -> title, subtitle -> content, and between major blocks within a step
